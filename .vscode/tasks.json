{
    "tasks": [
        {
            "type": "shell",
            "label": "Build RocketSim3D (MSVC)",
            "command": "cmd.exe",
            "args": [
                "/C",
                "temp_build.cmd"
            ],
            "options": {
                "cwd": "${workspaceFolder}"
            },
            "problemMatcher": [
                "$msCompile"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            }
        }
    ],
    "version": "2.0.0"
}
